# Обновление API клиента - Пагинация и WebP

## 📋 Краткое описание

Обновлена клиентская библиотека для работы с новым API v1.1.0:
- ✅ Добавлена поддержка пагинации
- ✅ Добавлены фильтры по площади и цене
- ✅ Поддержка изображений в формате WebP
- ✅ Сохранена обратная совместимость

## 🚀 Быстрый старт

### Без изменений (работает как раньше)

```javascript
const houses = await api.getHouses()
// Возвращает массив домов
```

### С пагинацией (новая функциональность)

```javascript
const result = await api.getHouses({ page: 1, limit: 20 })
// Возвращает: { data: [...], pagination: {...} }

console.log(result.data) // Массив домов
console.log(result.pagination.total) // Всего домов
console.log(result.pagination.hasNextPage) // Есть ли еще страницы
```

### С фильтрами

```javascript
const result = await api.getHouses({
  rooms: '3',
  areaMin: 80,
  areaMax: 120,
  priceMin: 3000000,
  priceMax: 5000000,
  page: 1,
  limit: 10
})
```

## 📝 Что изменилось

### Метод `api.getHouses(params?)`

**Параметры:**
- `page` - номер страницы
- `limit` - количество записей (max: 100)
- `offset` - смещение
- `rooms` - фильтр по комнатам
- `areaMin`, `areaMax` - фильтр по площади
- `priceMin`, `priceMax` - фильтр по цене

**Возвращаемое значение:**
- Без параметров → `Array<House>` (как раньше)
- С параметрами пагинации → `{ data: Array<House>, pagination: PaginationInfo }`

### Формат пагинации

```javascript
{
  page: 1,              // Текущая страница
  limit: 20,            // Записей на странице
  offset: 0,            // Смещение
  total: 42,            // Всего записей
  totalPages: 3,        // Всего страниц
  hasNextPage: true,    // Есть следующая страница
  hasPrevPage: false    // Есть предыдущая страница
}
```

## 🔄 Миграция существующего кода

### Вариант 1: Без изменений

Если вам не нужна пагинация, код продолжит работать как раньше:

```javascript
// ✅ Работает без изменений
const houses = await api.getHouses()
setHouses(houses)
```

### Вариант 2: С пагинацией

Если хотите использовать пагинацию:

```javascript
// Было
const houses = await api.getHouses()
setHouses(houses)

// Стало
const result = await api.getHouses({ page: 1, limit: 20 })
setHouses(result.data)
setPagination(result.pagination)
```

## 🖼️ Изображения WebP

Все изображения теперь автоматически конвертируются в WebP:
- Меньше размер (экономия 60-80%)
- Лучшее качество
- Быстрая загрузка страниц

Код не требует изменений, все URL остаются прежними.

## 📚 Полная документация

См. [API_USAGE.md](./API_USAGE.md) для подробной документации с примерами.

## ⚠️ Важно

- Обратная совместимость сохранена
- Существующий код продолжит работать
- Пагинация и фильтры - опциональные возможности
- Все методы возвращают безопасные значения при ошибках

## 🔗 Ссылки

- [Полная документация API](./API_USAGE.md)
- [Changelog сервера](./CHANGELOG_SERVER.md)
- Версия API: v1.1.0
- Дата: 23 февраля 2026
